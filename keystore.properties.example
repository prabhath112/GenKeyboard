# Copy to keystore.properties (gitignored). Generate the keystore with:
#   keytool -genkeypair -keystore genkeyboard-release.jks -alias genkeyboard -keyalg RSA -keysize 4096 -validity 10000
storeFile=../genkeyboard-release.jks
storePassword=
keyAlias=genkeyboard
keyPassword=
